# Installation

1. Install Emacs 27 or greater
2. `git clone git@github.com/jakeisnt/doom ~/.config/doom/`
3. `git clone --depth 1 https://github.com/doomemacs/core ~/.config/emacs && ~/.config/emacs/bin/doom install`
